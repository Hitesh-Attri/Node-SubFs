<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyAcc</title>
</head>
<body>
    <%- include('../partial/header', {username:username}); %>
    <h1>My Account</h1>
    <p>Username: <%= username %></p>
    <p>Email: <%= email %></p>
    <br>

    <ul>
        <% for(let i = 0 ; i < theFile.length; i++){ %>
            <li> <%= `User: ${theFile[i].username} , email: ${theFile[i].email}`  %> </li>
        <% } %>
    </ul>

    <a href="/logout">Logout</a>
</body>
</html>